{% extends 'backComp.html.twig' %}

{% block content %}
    <center>
        <div class="col-lg-6">
            <div class="card">
                <div class="card-header">
                    <strong class="card-title">Voici les données de paiement N:° : {{ paiement.id }}</strong>
                </div>
                <div class="card-body">
                    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Id Eleve</th>
            <td>{{ paiement.ideleve.id }}</td>
            </tr>
            <tr>
                <th>Typepaiement</th>
                <td>{{ paiement.typePaiement }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiement.montant }}</td>
            </tr>
        </tbody>
    </table>
    <center>
        <ul style="list-style-type: none;">
            <br>

            <li >
                {{ form_start(delete_form) }}
                <a href="{{ path('paiement_index') }}" class="btn btn-outline-secondary" >Retour à la liste</a>

                <a href="{{ path('paiement_edit', { 'id': paiement.id }) }}" class="btn btn-outline-warning" >Modifier</a>


                <input type="submit" value="Supprimer" class="btn btn-outline-danger">
                {{ form_end(delete_form) }}
            </li>

        </ul>
    </center>
    </div></div></div></center>
{% endblock %}

